<form method="post" action="{{ path('model_hairstyle_delete', {'id': coiffure.id}) }}" style="display: inline-block" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coiffure.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
